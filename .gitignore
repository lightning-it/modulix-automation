infra/
ansible-navigator.log
ansible_collections/

# Ansible Collections Development
collections-dev/ansible_collections/**
!collections-dev/.gitkeep
ansible/.vault-pass.txt

# AAP containerized installer archive (if stored in the repository for deployment).
ansible/*.tar.gz

# RPM packaging outputs
packaging/rpm/.rpmbuild/
packaging/rpm/dist/
ansible/ansible-automation-platform-containerized-setup-bundle-*.tar.gz
ansible/.ansible-runner/
ansible/.toolbox-podman/
